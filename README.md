# 离线图片与提示词管理库

选择一个本地图片文件夹，在 Codex 右侧浏览图片、管理标签和长提示词，按需分析或创作。不依赖 Eagle。

当前版本：`1.0.0+codex.20260912082137`。两个插件独立安装、独立运行，不自动互相同步。

## 下载和安装

1. Windows 电脑准备好 **Codex 桌面客户端、Python 3.10 或更新版，以及支持 `codex plugin add` 的 Codex CLI**。安装了桌面客户端不代表终端一定能找到 CLI。
2. [下载完整安装包](https://github.com/InkMore-AACC/Offline-Image-Prompt-Library/raw/refs/heads/main/downloads/offline-image-library-1.0.0.zip)，先完整解压，再进入有 `安装插件.cmd` 和 `install.py` 的那一层目录。不要只下载 plugin 子目录，也不要在压缩包内直接运行。
3. 双击 **安装插件.cmd**，等窗口明确显示“安装成功”。安装器会在你的用户目录生成本机路径，不使用作者电脑的路径。通常不需要管理员权限。
4. 在 Codex 中**新建任务**，发送：**打开离线图片与提示词管理库**。
5. 页面打开后点击“选择图库文件夹”，选择你自己的图片目录。

首次安装会创建独立 Python 环境并安装 Pillow，可能需要联网；图片浏览、索引、缩略图和元数据管理在本地运行。Codex 的 AI 分析和生图仍需要相应服务。

安装失败请看 [安装与排错](docs/安装与排错.md)。**预检查通过不等于安装成功；安装成功也不等于 Eagle 或图库连接已经验证。**

## 用法示例

- “读取我选中的图片，分析它们的提示词写法。”
- “分析这些图片的配色。”
- “用图1的配色、图2的构图，制作一张篮球海报。”
- “反推这张图片的提示词。”——完成后询问是否入库，确认后自动命名、分类并保存。

选中素材本身不会发送消息或开始生成。每个任务保存独立选择，不要复制其他任务带 `task=` 的完整网址。固定本地端口为 `18766`。

## 已实现的功能

- 原比例瀑布流、图片尺寸滑杆、自动加载、可见卡片数量限制，减少长列表负担。
- 一个搜索框搜索名称与标签；顶部文件夹选择与筛选中的文件夹选项同时保留。
- 颜色、标签、文件夹、形状组合筛选；选颜色时优先按匹配度排序。其他情况下按名称、日期或扩展名排序。
- 名称、扩展名、标签、提示词显示开关；名称与扩展名同一行显示。
- 单击选中并显示详情，双击图片放大；双击名称和标签编辑。卡片提示词双击编辑，详情和放大框提示词单击编辑。
- 点击编辑框外：没改动直接退出，有改动询问保存；详情栏可拖动宽度。
- 参考栏不限六张，缩略图保持比例，悬停展示名称、标签和提示词。
- 提示词反推后确认入库，保存前检查冲突，重试避免重复导入。

## 图片反推与保存规则

“图片反推”“看图写提示词”“还原/提取生成提示词”等意图均可触发 Skill，支持上传图片、截图、外部参考图，以及其他 Skill 已完成的反推。复用已有分析结果，不重复分析。触发依赖 Codex 识别并调用 Skill，不是强制拦截客户端所有对话。

反推完成后询问是否将本次图片、名称、标签和提示词存入图库。你确认后，Codex 根据画面内容拟定简短名称和分类标签，再调用保存工具；拒绝或未回答不写入。已有条目更新，外部本地原图复制入库，保留源文件。拿不到真实本地图片时不会假称导入成功。

离线版更新真实文件名；同名冲突会停止，不覆盖别的图片。提示词、标签写入可见的“图库资料/素材信息.json”。

两版都启用而目标不清楚时，先选择存到哪一版，不自动存两份。单纯看图识别、OCR、分析配色、阅读已有提示词和普通生图不额外触发反推。生成默认只借鉴已有提示词写法；只有明确要求参考原图，才向生图工具传入图片。本次创作改写不覆盖原图反推提示词。

## 数据与备份

安装包不含图库图片、个人提示词、选图记录或个人配置。图片放在你自己选择的文件夹，备份时复制整个图库文件夹，包括可见的“图库资料”子目录。

插件安装在 `%USERPROFILE%\plugins\offline-image-library`。运行配置及独立 Python 环境位于 `%USERPROFILE%\Documents\Codex\OfflineLibraryData`。 升级重新运行安装器，图库资产不随插件覆盖；安装器保留旧插件备份。不要把运行配置提交到 GitHub。

## 文档

- [完整功能与资料说明](使用说明.md)
- [安装、升级与常见错误](docs/安装与排错.md)
- [当前验证范围](docs/VALIDATION.md)
- [给 Codex 的仓库工作说明](AGENTS.md)

全新电脑安装和完整鼠标交互尚未逐项验收，不能把自动化测试当成所有机器兼容保证。
