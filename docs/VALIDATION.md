# 当前验证记录

发布版本：`1.0.0+codex.20260912082137`。

- 33 项 Python 自动化测试通过，覆盖保存确认、长提示词、元数据冲突、重复重试和既有回归场景。测试使用临时图片/模拟接口，不写真实私人图库。
- Skill 和插件结构校验通过；新版工具清单可读取，本机已安装并启用。
- 安装器 --check 已实跑，可执行 CLI 版本及插件命令检查；安装器改为调用已解析的 codex.cmd，安装后回读注册结果。
- 完整鼠标交互、全新电脑从零安装和新入库流程的真实 Eagle 写入（仅 Eagle 版）未在本次发布中逐项验收。

测试入口：使用有 Pillow 的 Python，运行 python -m unittest discover -s plugin/tests -p "test*.py"。

安装器检查通过只代表预检查，不代表缓存注册或服务连接完成。GitHub 发布后以对应 main 提交和下载包内容为准。
