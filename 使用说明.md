# 离线图片与提示词管理库

浏览本地图片，管理标签与提示词，辅助分析和创作。

## 打开

在 Codex 中说“打开离线图片与提示词管理库”。新版独立运行在127.0.0.1:18766，Eagle版仍在18765。每个对话使用自己的task参数，不复制其他对话完整网址。首次使用点击“选择图库文件夹”，也可直接告诉Codex本地文件夹路径。

两个图库的数据相互独立，不自动双向同步。

## 文件与资料

图片和子文件夹位于你选择的图库根目录。可见的“图库资料”目录保存素材信息.json、元数据备份、缩略图缓存和任务选择。不要只备份图片，搬家时复制整个图库根目录。

保存先写临时文件再替换，保留一份上次索引。长提示词已通过百万字符级别保存、重启重读及HTTP回读测试；单次HTTP提交上限64 MiB，不设置原 Eagle 版的10万字符限制。提示词在磁盘上是UTF-8文本，页面仅显示摘要，编辑读取全文。

图库后台每2秒扫描文件，页面每3秒检测变化。文件连续两轮尺寸及修改时间一致后读取；支持 JPG/JPEG、PNG、WebP、GIF、BMP、TIFF、ICO、AVIF（以Pillow解码支持为准）。其他文件保留但不展示；不跟随符号链接和目录联接。资料目录不参与扫描。

新图生成本地缩略图和颜色统计，不调用AI。文件改名或在库内移动时，仅在内容唯一对应时保留身份和资料；重复图片无法唯一判断时保留旧资料并提示，不乱配。图片持续缺失60秒后清理元数据、缓存和选择；根目录或资料目录不可访问时暂停清理。上一份备份可能暂时包含已删除项。

## 界面

沿用原图库原比例瀑布流、预览大小滑杆、自动加载、最多120张可见卡片。统一搜索名称和标签，顶部文件夹选择与组合筛选并存；颜色匹配优先，名称/文件日期/类型排序，颜色/标签/文件夹/形状组合筛选。

单击卡片显示详情；双击图片放大；双击名称和标签原位编辑。卡片提示词双击编辑，详情及放大框提示词单击编辑；点击外部未修改则退出，已修改询问保存。名称保存会重命名真实图片，不覆盖同名文件。右侧详情可拖动宽度。

选图不限制六张。顶部缩略图按比例显示，多图横向滚动；悬停展开与卡片同宽的信息预览，始终显示名称、标签和提示词，并出现取消按钮。

## Skill规则

选中不等于生成。可以让Codex读取文件、分析提示词、分析配色或按要求创作。生成默认只借鉴提示词写法，明确要求用图片作参考时才传原图。用本Skill实际反推图片后，Codex每次会询问是否保存本轮图片、分类标签和反推提示词；同意后自动起简洁中文名称、分类并调用专用保存工具。库内素材更新原条目，外部本地原图复制到当前图库，原文件保留；仅为生图改写的提示词不会覆盖原图分析。拒绝或未回答不写入。离线与Eagle同时启用时只保存到明确指定或当前使用的一处图库，不双重保存；这不是所有对话的全局拦截规则。

保存接口为 `offline_library_save_analysis`，要求 `confirmed:true` 和稳定的 `requestId`。同一请求重试不重复复制或覆盖后续编辑；同名文件、重复图片和过期原值会停止写入。返回结果包含磁盘回读验证的名称、标签、完整提示词、图片及元数据路径。离线指图库不依赖Eagle和互联网，Codex分析与生图仍需要相应在线服务。

## 安装与维护

安装包解压后双击“安装插件.cmd”。需要Windows、Python3.10+和Codex CLI。安装脚本创建独立Python运行环境并安装Pillow（首次安装可能联网，优先清华镜像，失败回退官方PyPI），不会修改Eagle插件。日常图库运行不联网。安装后新建任务以加载Skill。

元数据损坏时停止读取，不覆盖损坏文件。可从“图库资料/元数据备份/素材信息.json.previous”恢复，先关闭本插件服务再操作。重复图片自动关联歧义需要后续人工确认，不承诺自动匹配所有外部改动。


## 安装与故障入口

完整安装步骤与常见问题见 [安装与排错](docs/安装与排错.md)，当前验证范围见 [验证记录](docs/VALIDATION.md)。

## 视频功能

视频与图片使用同一套文件夹、名称、分类标签和提示词字段。直接用文件夹筛选，文件类型排序按原始扩展名排列，不增加“全部／图片／视频”入口。

视频封面右下角显示半透明 video 标记，没有卡片播放按钮。鼠标停留超过 0.5 秒后在卡片内静音循环播放，移开即停止并恢复封面；单击仅显示右侧文字资料，右侧栏不预览视频。双击封面打开放大播放器，支持播放、暂停、进度拖动、音量和全屏。关闭、隐藏页面或移除卡片会停止预览。提示词字段仅保存提示词正文，分类使用标签。

支持 MP4、WebM、MOV、MKV、AVI、M4V、MPEG/MPG、TS/MTS/M2TS、OGV。浏览器支持的编码直接播放；其他编码通过本地 FFmpeg 生成 H.264/AAC MP4 播放缓存，原文件、扩展名和排序保持原样。生成预览不调用 AI、不联网，第一次转换可能需要等待。

需要 FFmpeg 和 ffprobe 位于 PATH；未安装时图片功能仍可用，离线版会提示视频索引依赖缺失。安装器不会自动安装 FFmpeg，可从 FFmpeg 官方下载页安装 Windows 构建后重开图库服务。

## 随插件发布的参考素材

完整仓库安装包随附 `plugin/assets/image2.5参考`：541 张原图及 `manifest.json` 中的名称、标签和完整提示词。安装器复制这些随附素材并校验原图 SHA-256；安装后选择自己的图库，点击“导入随附参考素材”，即可导入平铺的 `image2.5参考` 文件夹。相同内容跳过；同名不同内容保留并另起文件名；导入不会覆盖已有修改。

请下载 GitHub 完整仓库 main.zip，素材随该安装包发布；旧 downloads 目录的小安装包不再作为发布入口。随附图片和提示词用于参考，其权利仍属于原作者，随附不构成额外授权。
