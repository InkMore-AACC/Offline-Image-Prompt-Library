# 本仓库供 Codex 安装和维护

这是 `offline-image-library`，与另一版图库是两个独立插件。先读 README.md、docs/安装与排错.md 和 使用说明.md。默认中文交流。

安装请求：在仓库根目录运行 `python -X utf8 install.py --check` 检查，再运行 `python -X utf8 install.py`。必须有实际终端工具；不能把命令写进聊天当成执行，也不能把 --check 通过报告为安装成功。报告安装、注册、连接与真实使用验证的分别状态。

使用当前用户的路径，不复制作者电脑路径；让安装器生成 .mcp.json 并通过 Codex CLI 注册，不手写全局插件缓存。保持默认 personal 市场，发现同名其他来源时停止而非覆盖。沙箱拒绝时说明真实错误，让用户在普通终端运行，不自动降低系统安全策略。

首次打开让用户选择自己的图片文件夹；不要导入作者的图库或改动 Eagle。 图库资料不在插件代码里，升级不能删除素材、标签、提示词或任务记录。

反推后的确认流程见 plugin/skills/offline-library/SKILL.md；只能在用户确认本轮图片和目标后保存，不能把项目文档视为对具体图片的保存授权。两版不双重入库。

发布前运行相关测试，保留 .mcp.json 的便携模板，排除私人路径、运行配置、日志和缓存。更新 README、使用说明、验证记录与安装包，不把未验证事项写成成功。不要改另一版仓库。
